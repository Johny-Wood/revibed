import styles from './styles.module.scss';

function ProjectCardVideoSectionLayout({ children }) {
  return <div className={styles.projectCardVideoSection}>{children}</div>;
}

export default ProjectCardVideoSectionLayout;
