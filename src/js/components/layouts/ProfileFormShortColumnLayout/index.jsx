import styles from './styles.module.scss';

function ProfileFormShortColumnLayout({ children }) {
  return <div className={styles.formSettingsContent_shortColumn}>{children}</div>;
}

export default ProfileFormShortColumnLayout;
